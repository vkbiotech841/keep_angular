// Making interface with fields
export interface Note {       // Note is an object.
    title?: string;
    description?: string;     // and images and convasImages as arrays.
    images?: any[];
    canvasImages?: any[];
};